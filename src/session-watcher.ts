/**
 * Session watcher — Phase 2.
 *
 * Tails ~/.claude/projects/<munged>/*.jsonl files to surface events for
 * sessions the user already has running outside the bridge ("adopt" mode).
 *
 * Phase 1 stub: no-op so the bridge module structure compiles and is in place
 * for the Phase 2 add.
 */

export class SessionWatcher {
  start(): void {
    // intentional no-op for Phase 1
  }
  stop(): void {
    // intentional no-op for Phase 1
  }
}
